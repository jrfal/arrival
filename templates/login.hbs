<!DOCTYPE html>
<html lang="en">

<head>
  <title>{{signIn}}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width" />
</head>

<body class="bg-light">
  <div class="container min-vh-100 d-flex flex-column justify-content-center py-4 align-items-center">
    <main class="card col-12 col-sm-8 col-md-6 col-lg-4 shadow-sm rounded-3">
      <div class="card-body p-4">
        <h1 class="mb-4">{{signIn}}</h1>
        {{#if badCreds}}
        <div class="alert alert-danger" role="alert">
          Incorrect username or password.
        </div>
        {{/if}}
        <form>
          <div class="mb-3">
            <label for="username" class="form-label mb-1">Username</label>
            <input class="form-control{{#if requiredMessages}} is-invalid{{/if}}" type="text" id="username" autocomplete="username" />
            {{#if requiredMessages}}
            <div class="invalid-feedback">Enter your username.</div>
            {{/if}}
          </div>
          <div class="mb-3">
            <label for="password" class="form-label mb-1">Password</label>
            <input class="form-control{{#if requiredMessages}} is-invalid{{/if}}" type="password" id="password" autocomplete="current-password" />
            {{#if requiredMessages}}
            <div class="invalid-feedback">Enter your password.</div>
            {{/if}}
          </div>
          <div class="d-grid mt-4">
            <button class="btn btn-primary" type="submit">Continue</button>
          </div>
        </form>
        <a href="{{forgotPWUrl}}">Forgot Password</a> &bull; <a href="{{registrationUrl}}">Register</a>
      </div>
    </main>
  </div>
</body>

</html>